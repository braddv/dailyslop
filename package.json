{
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "build": "node --check api/_lib/backtests.js && node --check api/_lib/regimes.js && node --check api/signal-history.js"
  },
  "dependencies": {
    "@neondatabase/serverless": "^0.10.4",
    "@vercel/edge-config": "^1.4.0",
    "@vercel/functions": "^3.7.5"
  }
}
